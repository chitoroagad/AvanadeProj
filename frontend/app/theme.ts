import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    background: {
      default: "#f0f2f5",
    },
    primary: {
      main: "#ACE0F9",
    },
    secondary: {
      main: "#FFB6C1",
    },
  },
});

export default theme;
